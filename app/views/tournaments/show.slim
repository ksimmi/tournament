h1 = tournament.title
br

h3 Tournament's divisions
br

= render partial: 'divisions', locals: { divisions: divisions }

- unless playoff.nil?
  = render partial: 'playoff', locals: { playoff: playoff }